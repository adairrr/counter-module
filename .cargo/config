[alias]
wasm = "build --package counter-app --release --target wasm32-unknown-unknown"
unit-test = "test --lib"
schema = "run --example schema"
deploy = "run --package scripts --bin deploy_app"
#optimize =

[env]
ABSTRACT_VERSION = "0.2.0-beta.3"
